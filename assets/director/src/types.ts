export type Format = '3x4'|'9x16'|'16x9';
export type Shot = {id:string;component:string;from:number;end:number;durationFrames:number;transitionIn:{type:string;frames:number};props?:Record<string,unknown>;layouts:Record<Format,Record<string,unknown>>;assets?:string[];viewerGain?:string};
export type ShotProps = {shot:Shot;format:Format;layout:Record<string,unknown>;events:Record<string,number>;style:Record<string,unknown>;cue:(name:string)=>number;asset:(id:string)=>string;};
export type Timeline = {draft:boolean;fps:number;durationFrames:number;formats:Record<Format,{width:number;height:number}>;events:Record<string,number>;style:Record<string,unknown>;assets:Record<string,{src:string}>;globalAssets:string[];shots:Shot[];captionStyle:Record<string,unknown>;captions:Array<{text:string;from:number;end:number}>;voices:Array<{id:string;src:string;from:number;durationFrames:number;gain:number;real:boolean}>;audio:Array<{src:string;from:number;end:number;durationFrames:number;trimStart:number;gain:number;fadeIn:number;fadeOut:number;duck?:number}>};
