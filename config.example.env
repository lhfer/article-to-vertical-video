# Copy to config.env next to your <workdir> (or anywhere) and load it before step 6 (voice-over):
#   set -a; source /path/to/config.env; set +a
# Never commit config.env.

# 火山引擎 豆包语音 Seed-TTS 2.0 (X-Api-Key). Unlocks scripts/tts_seed2.py.
SEED_AUDIO_KEY=

# Optional. xAI REST Imagine (only if you choose --provider grok-rest; the default grok-cli path uses `grok login`).
# XAI_API_KEY=
